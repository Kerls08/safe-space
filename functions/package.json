{
  "name": "safe-space-functions",
  "version": "0.0.1",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node test/run_test.js",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.6",
    "firebase-admin": "^11.9.0",
    "firebase-functions": "^4.3.0"
  },
  "devDependencies": {
    "@types/node": "^18.16.19",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": "18"
  }
}
